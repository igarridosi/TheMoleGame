<Window x:Class="Client.CreateUserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Client"
        mc:Ignorable="d"
        Title="Erabiltzailea Sortu" Height="400" Width="400"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="#1E1E1E">

    <Border Padding="25">
        <StackPanel>
            <TextBlock Text="ERABILTZAILE BERRIA" Foreground="#00ADB5" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>

            <!-- IZENA -->
            <TextBlock Text="Izena:" Foreground="Gray" Margin="0,0,0,5"/>
            <TextBox x:Name="txtUser" Height="35" Background="#2D2D2D" Foreground="White" BorderBrush="#00ADB5" Padding="5" Margin="0,0,0,15"/>

            <!-- PASAHITZA -->
            <TextBlock Text="Pasahitza:" Foreground="Gray" Margin="0,0,0,5"/>
            <PasswordBox x:Name="txtPass" Height="35" Background="#2D2D2D" Foreground="White" BorderBrush="#00ADB5" Padding="5" Margin="0,0,0,15"/>

            <!-- ROLA (ComboBox) -->
            <TextBlock Text="Rola:" Foreground="Gray" Margin="0,0,0,5"/>
            <ComboBox x:Name="cmbRole" Height="35" Margin="0,0,0,25" FontSize="14" VerticalContentAlignment="Center">
                <ComboBoxItem Content="Player" IsSelected="True"/>
                <ComboBoxItem Content="Admin"/>
                <ComboBoxItem Content="Moderator"/>
            </ComboBox>

            <!-- BOTOIA -->
            <Button Content="SORTU" Height="40" Background="#00ADB5" Foreground="White" FontWeight="Bold" Click="BtnCreate_Click"/>
        </StackPanel>
    </Border>
</Window>
